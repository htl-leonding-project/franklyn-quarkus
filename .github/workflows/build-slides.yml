name: Build
on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: quirinecker/asciidoctor-convert-action@1.0
        with:
          slides: true
          inputPath: input
          outputPath: output
      - run: |
          #sudo chmod +x ./docker-scripts/docker-convert.sh
          #sudo ./docker-scripts/docker-convert.sh